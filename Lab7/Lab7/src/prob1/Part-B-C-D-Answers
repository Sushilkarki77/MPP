B. It neither overrides equals nor hashcode.
C. It does overrides equals but hash code is not overridden.
D. It overrides equals and hash code but property visited causing an issue, removing it from the class and removeDuplicates method should fix the problem.